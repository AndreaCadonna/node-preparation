/**
 * Exercise 1: Create a Simple Event Emitter
 *
 * Task:
 * Create a simple bell that emits a 'ring' event when the ring() method is called.
 * Set up a listener that logs "Ding dong!" when the bell rings.
 *
 * Requirements:
 * 1. Create a Bell class that extends EventEmitter
 * 2. Implement a ring() method that emits a 'ring' event
 * 3. Add a listener that responds to the ring event
 * 4. Test by calling ring() multiple times
 */

const EventEmitter = require('events');

// YOUR CODE HERE
// Create the Bell class

// Create an instance

// Add a listener for the 'ring' event

// Test: Ring the bell multiple times


/*
 * Expected output:
 * Ding dong!
 * Ding dong!
 * Ding dong!
 */

// After completing, compare with: solutions/exercise-1-solution.js
